# 数字艺术学院新闻循环展示系统开发说明书

## 1. 项目概述与目标

建设面向学院内部使用的轻量级新闻循环展示网站。宣传工作人员完成新闻排版并导出 PDF，管理员上传 PDF 后，系统按页生成展示图片，通过大厅电脑全屏循环播放。

系统以功能完整、运行稳定、操作简单、部署方便为目标，实现以下能力：

1. 上传单页或多页 PDF 稿件，保存原始文件。
2. 按“一页 = 一篇新闻”拆分并生成高清 WebP 图片和后台缩略图。
3. 管理员预览、加入播放、下架、删除新闻，拖拽调整播放顺序。
4. 保留部分历史新闻，与新上传的新闻共同播放。
5. 大厅自动全屏循环展示，定时同步后台的播放列表和展示设置。
6. 配置停留时间、切换动画、学院 Logo 和默认宣传画面。
7. 大厅电脑开机后自动进入展示页面，减少工作人员日常操作。

本说明书作为项目实现、联调、部署和验收依据。下文的字段类型、请求结构与模块划分用于落地上述功能。

## 2. 用户角色

| 角色 | 使用入口 | 职责 |
|---|---|---|
| 管理员 | `/admin` | 登录、上传、预览、管理新闻与播放顺序、修改展示设置 |
| 大厅展示端 | `/display` | 公开访问展示内容，自动获取播放列表并循环播放 |

管理员通过用户名和密码登录，密码以哈希形式保存。后台业务接口验证管理员登录状态。大厅端直接访问公开展示接口。

## 3. 核心业务规则

### 3.1 一页对应一篇新闻

一个 PDF 文件包含 N 页，成功转换后按原有页码生成 N 条独立新闻记录。每条新闻独立控制播放状态。

新闻图片保留稿件中已经完成的标题、正文、插图及版式。后台以“原文件名 + 第几页”标识新闻。

### 3.2 文件与新闻页面

原始文件是新闻页面的来源，播放列表由新闻页面构成。转换成功后的新闻初始状态为 `OFFLINE`，管理员通过预览确认效果，再加入播放。

### 3.3 日期与播放状态

上传日期用于历史查询；播放状态决定是否进入大厅播放列表。不同日期新闻可以混合播放。

例如：昨日新闻 A 保持播放，昨日新闻 B 下架，今日新闻 C、D 加入播放后，播放顺序为 `A → C → D → A……`。

### 3.4 加入、下架与排序

- 单篇加入播放：追加至当前播放列表末尾；重复操作保持原有位置。
- 全部加入播放：将所有转换成功且处于 `OFFLINE` 状态的新闻加入播放列表，保留现有播放项及其顺序。追加项按上传时间、来源文件 ID、页码升序排列。
- “全部加入播放”作用于全部符合条件的新闻，后台按钮旁说明作用范围及数量。
- 单篇下架：改为 `OFFLINE`，保留新闻与原始文件，剩余播放顺序重新整理。
- 全部下架：确认后将全部播放新闻改为 `OFFLINE`。
- 拖拽排序：保存当前完整播放列表的顺序，`sort_order` 从 1 开始连续编号。
- 历史新闻再次加入播放时，仍追加至列表末尾。

### 3.5 删除

下架用于保留新闻以便再次使用；删除用于永久清理数据。删除操作显示确认提示。

- 删除单篇新闻：删除对应页面记录、高清图片和缩略图；该新闻处于播放状态时同步移出播放列表并整理顺序。来源文件继续保留。
- 删除来源文件：确认提示列明关联新闻数量，删除来源文件及其关联新闻、转换产物，并整理播放列表。
- 文件页数记录原始稿件的总页数，保留新闻数量按现存新闻记录统计。

播放状态与顺序的修改在数据库事务中完成，确保大厅查询得到一致的播放列表。

## 4. 业务流程

```text
管理员登录
  → 上传 PDF
  → 校验并保存原文件
  → 创建来源文件记录，状态为 PROCESSING
  → 识别 PDF 页数并逐页处理
  → 逐页生成高清 WebP 与缩略图
  → 创建独立新闻记录，状态为 OFFLINE
  → 文件转换状态变为 SUCCESS
  → 管理员预览
  → 单篇或全部加入播放
  → 拖拽调整播放顺序
  → 大厅自动同步并循环展示
```

转换异常时，将来源文件状态设为 `FAILED`，后台展示失败原因与重新上传提示。管理员日常通过“上传新稿件、保留旧新闻、下架过期新闻、调整顺序”维护大厅内容。

## 5. 后台功能与页面结构

### 5.1 页面路由

| 页面 | 路由 | 主要内容 |
|---|---|---|
| 登录 | `/admin/login` | 用户名、密码、登录结果提示 |
| 播放管理 | `/admin/play` | 当前播放数量、有序新闻列表、预览、下架、全部下架、拖拽排序 |
| 新闻管理 | `/admin/news` | 上传入口、文件转换状态、历史新闻、预览、加入播放、全部加入播放、删除 |
| 展示设置 | `/admin/settings` | 停留时间、动画、Logo、默认宣传画面 |
| 大厅展示 | `/display` | 新闻画面、统一背景及配置的 Logo |

访问 `/admin` 时，已登录管理员进入播放管理，其他访问进入登录页。后台采用左侧导航、右侧工作区布局，关键操作完成后显示结果提示。

### 5.2 登录

提供用户名和密码登录、退出登录。登录失效后进入登录页。初始管理员账号通过部署初始化建立，初始密码由部署环境配置。

### 5.3 播放管理

按实际播放顺序展示新闻，列表包含拖拽手柄、缩略图、来源文件名、页码、上传日期以及预览和下架操作。

拖拽结束后保存顺序；保存失败时显示提示并恢复服务器上的顺序。全部下架操作确认后执行，列表为空时提示大厅将显示默认宣传画面。

### 5.4 新闻管理

- 支持选择或拖入 `.pdf` 文件上传；上传区域明确提示“仅支持 PDF”。
- 上传后显示原文件名称、上传时间、页数、转换状态和失败提示。
- 转换中定时查询处理状态，完成后展示各新闻页的缩略图。
- 点击预览查看高清图片，核对字体、分页、图片和版式。
- 按上传时间查看历史新闻，支持日期筛选与分页浏览。
- 显示新闻播放状态，提供单篇加入播放、下架、删除及全部加入播放。
- 来源文件列表提供删除文件入口，并明确关联新闻的删除范围。

### 5.5 展示设置

| 配置项 | 默认值或行为 | 后台操作 |
|---|---|---|
| 单页停留时间 | 10 秒 | 输入正数秒值 |
| 切换动画 | 开启 | 开启或关闭 |
| 动画类型 | `fade`，淡入淡出 | 选择已实现的动画类型；基础实现提供淡入淡出 |
| 动画时长 | 0.8 秒 | 输入正数秒值，且小于停留时间 |
| Logo 显示 | 开启；配置图片后显示 | 开启或关闭 |
| Logo 图片 | 管理员上传 | 上传、更换、预览 |
| Logo 位置 | 左上角 | 左上、右上、左下、右下 |
| 默认宣传画面 | 系统内置基础宣传图 | 上传、更换、预览 |

Logo 由网页统一叠加，保持比例并留出边距。稿件已包含 Logo 时，可通过设置关闭叠加。图片上传校验实际图像内容，成功后保存到本地资源目录。

设置保存后，大厅通过定时同步自动应用。

## 6. 大厅展示

### 6.1 画面与比例

以 16:9、1920 × 1080 为目标展示画面。每次完整展示一页新闻，稿件保持原始宽高比，采用等比例包含方式呈现，剩余区域使用统一背景填充。A4 竖版等稿件同样完整展示。

界面仅呈现新闻、背景和配置的 Logo。鼠标指针自动隐藏。浏览器通过全屏启动方式占满显示屏。

### 6.2 自动轮播

进入页面后读取展示设置与播放列表，预加载图片，按 `sort_order` 升序循环播放。默认每页完整停留 10 秒，再按配置完成切换动画；关闭动画时直接切换。只有一篇新闻时持续显示该页。

### 6.3 自动同步

每约 30 秒请求一次最新播放列表及展示配置：

1. 内容保持一致时，继续当前播放进度。
2. 内容变化时，加载新增图片，在下一次页面切换时应用新列表与配置。
3. 播放列表变空时，切换至默认宣传画面。
4. 默认画面展示期间继续轮询，新增播放新闻后自动恢复轮播。
5. 请求短暂失败时，继续播放当前浏览器页面已经加载成功的内容，并按周期重试。

单张图片加载失败时跳过该项并继续播放可用新闻；所有新闻图片均不可用时显示默认宣传画面。系统随程序提供基础宣传图，作为初始展示资源。

## 7. 文件转换与本地存储

### 7.1 转换管线

```text
PDF ── 页数识别 ── 逐页渲染 ── 高清 WebP + 缩略图
```

后端 `converter` 模块封装 PDF 页数识别、逐页渲染及 WebP 编码。PDF 渲染和 WebP 编码使用与后端运行环境兼容的工具或库，并在项目依赖和部署说明中固定具体版本。

### 7.2 处理步骤

1. 校验 `.pdf` 扩展名、文件实际类型、PDF 可读取性与可配置的上传大小限制；其他格式明确提示仅支持 PDF。
2. 以唯一文件名保存原始文件，原始名称单独记录在数据库。
3. 创建 `source_file` 记录，状态为 `PROCESSING`，返回文件 ID。
4. 通过后端进程内后台任务执行转换，控制同时运行的转换数量。
5. 读取 PDF 页数，按原文件页码逐页处理。
6. 按 PDF 页码生成高清 WebP 与缩略图，记录每页对应路径。
7. 全部页面生成完成后，一次性写入新闻记录并标记文件为 `SUCCESS`。
8. 失败时清理本次未完成产物、保存错误信息并设为 `FAILED`，原始上传文件保留。

转换调用设置超时时间，异常和超时均结束处理并提示失败。服务启动时检查遗留的 `PROCESSING` 记录，核实任务中断后标记失败，供管理员重新上传。

### 7.3 图片质量与版式

高清图片按目标屏幕和原稿比例渲染，保证 1920 × 1080 展示环境中的文字与图片清晰。后台缩略图使用较小尺寸以提高列表加载速度。

宣传工作人员导出 PDF 时应嵌入所用字体，并在上传前确认分页、图片和版式。使用真实 PDF 对照原稿检查转换结果，管理员通过转换后预览确认展示效果。

### 7.4 存储结构

```text
uploads/
├── originals/       # 原始 PDF
├── pages/           # 按来源文件 ID 分组的高清 WebP
├── thumbnails/      # 对应的缩略图
├── assets/          # Logo、默认宣传图片
└── temp/            # 转换工作目录，完成后清理
```

数据库保存相对于存储根目录的路径，API 输出浏览器可访问的资源 URL。原文件和转换临时文件通过受控方式访问，公开展示资源通过专用资源路径提供。删除和路径解析均限定在配置的存储根目录内。

## 8. MySQL 四表设计

数据库采用 MySQL，字符集为 `utf8mb4`。主键使用 `BIGINT`；时间统一按学院所在地时区记录和展示。数据库字段采用下划线命名，API 字段采用小驼峰命名。

### 8.1 `admin_user`：管理员账号

| 字段 | 类型 | 约束与说明 |
|---|---|---|
| id | BIGINT | 主键 |
| username | VARCHAR(64) | 非空、唯一 |
| password_hash | VARCHAR(255) | 非空，保存密码哈希 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 修改时间 |

### 8.2 `source_file`：上传文件

| 字段 | 类型 | 约束与说明 |
|---|---|---|
| id | BIGINT | 主键 |
| original_name | VARCHAR(255) | 原始文件名称 |
| file_path | VARCHAR(512) | 原文件相对路径 |
| page_count | INT | 转换成功时写入原文件总页数，初始为 0 |
| convert_status | VARCHAR(20) | `PROCESSING`、`SUCCESS`、`FAILED` |
| error_message | TEXT | 失败原因，可空 |
| uploaded_by | BIGINT | 关联 `admin_user.id` |
| created_at | DATETIME | 上传时间 |
| updated_at | DATETIME | 状态更新时间 |

按 `created_at` 建立索引，支持历史文件查询。

### 8.3 `news_page`：新闻页面

| 字段 | 类型 | 约束与说明 |
|---|---|---|
| id | BIGINT | 主键 |
| source_file_id | BIGINT | 关联 `source_file.id` |
| page_number | INT | 来源页码，从 1 开始 |
| image_path | VARCHAR(512) | 高清图片相对路径 |
| thumbnail_path | VARCHAR(512) | 缩略图相对路径 |
| play_status | VARCHAR(20) | `PLAYING`、`OFFLINE`，默认 `OFFLINE` |
| sort_order | INT | 播放项为连续正整数，下架项为 0 |
| created_at | DATETIME | 新闻记录创建时间 |
| updated_at | DATETIME | 修改时间 |

建立 `(source_file_id, page_number)` 唯一约束，以及 `(play_status, sort_order)` 索引。历史日期筛选采用来源文件的上传时间。

大厅查询：

```sql
SELECT n.*
FROM news_page n
JOIN source_file s ON s.id = n.source_file_id
WHERE n.play_status = 'PLAYING'
  AND s.convert_status = 'SUCCESS'
ORDER BY n.sort_order ASC, n.id ASC;
```

### 8.4 `display_config`：展示配置

采用单行配置，固定 `id = 1`。

| 字段 | 类型 | 默认值与说明 |
|---|---|---|
| id | BIGINT | 主键，固定为 1 |
| display_seconds | INT | 10，单页停留秒数，正整数 |
| animation_enabled | BOOLEAN | true |
| animation_type | VARCHAR(32) | `fade` |
| animation_duration | DECIMAL(5,2) | 0.80，单位秒 |
| logo_enabled | BOOLEAN | true |
| logo_path | VARCHAR(512) | Logo 相对路径，可空 |
| logo_position | VARCHAR(20) | `top-left` |
| default_image_path | VARCHAR(512) | 默认宣传图片相对路径，初始化时提供 |
| updated_at | DATETIME | 修改时间 |

`logo_position` 支持 `top-left`、`top-right`、`bottom-left`、`bottom-right`。同步周期在应用参数中设为 30 秒。

数据关系：`admin_user 1 → N source_file`，`source_file 1 → N news_page`；`display_config` 保存全局展示设置。

## 9. API 设计

### 9.1 通用约定

- API 前缀为 `/api`，数据请求采用 JSON，文件上传采用 `multipart/form-data`。
- 登录接口及 `/api/display/*` 公开访问，其余业务接口校验管理员会话。
- 登录成功由后端建立会话，前端随请求携带会话 Cookie；退出登录使会话失效。
- JSON 响应统一包含 `code`、`message`、`data`，HTTP 状态码表达成功、参数错误、未登录、资源不存在及服务器异常。
- 分页参数为 `page`、`pageSize`；日期筛选为 `dateFrom`、`dateTo`；分页结果包含 `items`、`total`。
- BIGINT 标识在 JSON 中按字符串传输。

### 9.2 接口清单

| 方法 | 路径 | 功能与主要输入输出 |
|---|---|---|
| POST | `/api/auth/login` | 输入 `username`、`password`，登录成功返回管理员基本信息 |
| POST | `/api/auth/logout` | 退出登录 |
| POST | `/api/files/upload` | 文件字段 `file`；返回文件 ID 与 `PROCESSING` 状态 |
| GET | `/api/files` | 分页查询来源文件及转换状态，支持上传日期筛选 |
| GET | `/api/files/{id}` | 返回文件详情、页数、转换状态、失败提示 |
| DELETE | `/api/files/{id}` | 删除原文件及关联新闻、转换产物 |
| GET | `/api/news` | 分页查询新闻，支持 `sourceFileId`、`playStatus`、上传日期筛选 |
| DELETE | `/api/news/{id}` | 删除单篇新闻及对应图片 |
| PUT | `/api/news/{id}/play` | 单篇加入播放 |
| PUT | `/api/news/{id}/offline` | 单篇下架 |
| PUT | `/api/news/play-all` | 所有成功转换的下架新闻追加到播放末尾，返回新增数量 |
| PUT | `/api/news/offline-all` | 全部下架，返回处理数量 |
| PUT | `/api/news/sort` | 输入 `ids` 数组，按数组顺序保存当前完整播放列表 |
| GET | `/api/config` | 获取展示设置 |
| PUT | `/api/config` | 修改停留时间、动画及 Logo 显示位置等参数 |
| POST | `/api/config/logo` | 上传 Logo，返回图片 URL 并更新配置 |
| POST | `/api/config/default-image` | 上传默认宣传图片并更新配置 |
| GET | `/api/display/news` | 返回按播放顺序排列的完整新闻列表 |
| GET | `/api/display/config` | 返回大厅使用的展示参数及资源 URL |

播放管理页读取完整有序播放列表，并结合后台接口执行修改。排序接口校验 ID 唯一性以及与当前播放集合的一致性；出现并发变更时返回冲突提示，前端重新加载列表。

### 9.3 关键请求与响应示例

排序请求：

```json
{
  "ids": ["103", "101", "105"]
}
```

大厅播放列表响应：

```json
{
  "code": "OK",
  "message": "成功",
  "data": [
    {"id": "103", "imageUrl": "/media/pages/21/page_003.webp", "sortOrder": 1},
    {"id": "101", "imageUrl": "/media/pages/21/page_001.webp", "sortOrder": 2}
  ]
}
```

大厅配置响应中的 `data`：

```json
{
  "displaySeconds": 10,
  "animationEnabled": true,
  "animationType": "fade",
  "animationDuration": 0.8,
  "logoEnabled": true,
  "logoUrl": "/media/assets/logo.webp",
  "logoPosition": "top-left",
  "defaultImageUrl": "/media/assets/default-display.webp"
}
```

## 10. 技术架构

| 层次 | 技术 | 职责 |
|---|---|---|
| 前端 | Vue 3 + Element Plus | 后台界面、预览、排序、展示设置、大厅轮播 |
| 后端 | Spring Boot | 认证、文件上传、转换任务、新闻与播放管理、配置、API |
| 数据库 | MySQL | 管理员、来源文件、新闻页面、展示配置 |
| 文档转换 | PDF 渲染及 WebP 编码工具 | PDF 逐页生成图片 |
| 文件存储 | 服务器本地文件系统 | 原稿、高清图片、缩略图、展示资源 |

```text
管理员浏览器 /admin       大厅浏览器 /display
          └─────────┬─────────┘
                 Vue 3
                   │ HTTP API
              Spring Boot
          ┌────────┼─────────┐
        MySQL   本地文件目录   converter
                            └─ PDF 渲染 / WebP
```

后端按控制器、业务服务、数据访问、文件转换模块组织。部署时可由 Spring Boot 提供 Vue 构建后的静态页面与业务 API，通过同一地址访问后台和大厅。

## 11. 项目目录

```text
college-news-display/
├── frontend/
│   ├── package.json
│   └── src/
│       ├── views/
│       │   ├── admin/
│       │   │   ├── Login.vue
│       │   │   ├── PlayManage.vue
│       │   │   ├── NewsManage.vue
│       │   │   └── DisplaySetting.vue
│       │   └── display/Display.vue
│       ├── components/
│       ├── api/
│       ├── router/
│       └── stores/
├── backend/
│   ├── pom.xml
│   └── src/
│       ├── main/
│       │   ├── java/<项目包名>/
│       │   │   ├── controller/
│       │   │   ├── service/
│       │   │   ├── repository/
│       │   │   ├── entity/
│       │   │   ├── dto/
│       │   │   ├── config/
│       │   │   └── converter/
│       │   └── resources/
│       │       ├── application.yml
│       │       ├── db/schema.sql
│       │       └── static/
│       └── test/
├── uploads/
│   ├── originals/
│   ├── pages/
│   ├── thumbnails/
│   ├── assets/
│   └── temp/
├── deploy/
│   └── README.md
└── README.md
```

根目录 README 写明依赖版本、配置项、初始化方式、启动与构建命令、演示操作；部署说明记录服务器安装、大厅开机启动和常见故障排查。

## 12. 运行与部署

### 12.1 开发运行

1. 安装与项目版本匹配的 Node.js、JDK、构建工具、MySQL 及 PDF/WebP 处理依赖。
2. 执行数据库初始化，建立四张表、管理员账号和默认展示配置。
3. 配置数据库连接、文件存储根目录、上传限制及 PDF 渲染超时。
4. 启动 Spring Boot，再启动 Vue 开发服务，并配置 API 与资源请求代理。
5. 使用真实多页稿件完成“上传 → 转换 → 预览 → 加入播放 → 大厅展示”验证。

### 12.2 学院服务器部署

部署到学院的一台服务器或运行稳定的电脑，安装 MySQL、Java 运行环境及 PDF/WebP 处理依赖。

构建 Vue 静态页面和 Spring Boot 应用，配置前端路由回退，使直接访问 `/admin`、`/display` 均能正常打开。配置数据库和持久化的 `uploads` 目录，应用更新后继续使用已有数据和文件。

将数据库和应用配置为随系统启动运行，确保运行账号具有转换和文件读写权限。管理员与大厅电脑通过学院网络访问固定服务器地址：

```text
http://服务器IP:端口/admin
http://服务器IP:端口/display
```

部署交付时记录实际地址、软件版本、目录位置、启动停止方式和日志位置。学院已有域名和 HTTPS 环境时，可结合现有设施配置访问地址。

### 12.3 大厅电脑

大厅电脑使用 Windows，配置展示专用账户登录和 Edge 开机自动启动。启动参数示例：

```text
msedge.exe --kiosk http://服务器IP:端口/display --edge-kiosk-type=fullscreen
```

实际部署时替换服务器地址，并按安装位置配置 Edge 可执行文件路径。设置显示分辨率，调整休眠、屏幕关闭及屏幕保护策略，使宣传时段持续显示。

验证完整流程：`电脑开机 → Windows 登录展示账户 → Edge 全屏打开 /display → 自动循环播放`。

## 13. 七阶段开发计划

按 Codex 辅助开发安排，先形成可演示系统，再完成联调测试、部署与现场试运行。时间为工作量估算，转换环境与真实稿件适配结果用于调整进度。

| 阶段 | 预计时间 | 主要工作 | 阶段成果与检查点 |
|---|---|---|---|
| 1. 项目基础搭建 | 0.5 天 | Vue、Spring Boot、MySQL、四表初始化、登录、后台框架 | 系统启动成功，管理员可登录并进入后台 |
| 2. 稿件上传与转换 | 约 0.5 天 | PDF 上传、保存原稿、按页渲染、WebP、缩略图、状态与错误提示 | 多页 PDF 生成对应数量的独立新闻，可预览 |
| 3. 新闻与播放管理 | 0.5～1 天 | 历史查询、预览、删除、单篇与全部加入播放、单篇与全部下架、拖拽排序 | 后台业务可操作，跨日期新闻可共同播放 |
| 4. 大厅展示端 | 0.5 天 | 16:9 全屏、默认 10 秒、循环播放、动画、鼠标隐藏、等比例显示、定时同步 | 大厅轮播演示可运行，后台操作可自动同步 |
| 5. 展示设置 | 0.5 天 | 停留时间、动画开关与参数、Logo、位置、默认宣传图 | 完整功能 Demo，可演示全部主要功能 |
| 6. 联调与测试 | 1～2 天 | 真实 PDF、多页与版式检查、上下架、排序、设置、异常文件、长时间轮播、问题修复 | 验收用例通过，形成可部署候选版本 |
| 7. 部署与试运行 | 部署 1～2 天；试运行约 1 周 | 安装服务器环境、配置存储和 PDF 渲染依赖、大厅 Edge 全屏自启、真实稿件试用、现场问题修复 | 服务器与大厅开机流程通过，形成正式交付版本 |

进度节点：

- 核心演示目标：约 1～2 个工作日，跑通上传、转换、预览、加入播放、大厅轮播与下架。
- 完整 Demo：阶段 1～5 合计约 2～3 个工作日，对外安排约 2～3 个工作日。
- 联调测试：约 1～2 个工作日。
- 部署配置：约 1～2 个工作日。
- 现场试运行：约 1 周，记录并修复影响实际使用的问题。

每阶段完成对应检查点后进入下一阶段。阶段 5 是完整功能演示节点，阶段 6、7 是实际投入使用的交付节点。

## 14. 验收标准

| 编号 | 验收项目 | 操作与通过标准 |
|---|---|---|
| A01 | 登录与访问 | 正确账号可登录，错误密码提示失败；未登录访问后台业务接口受到认证控制；大厅可直接访问 |
| A02 | PDF 稿件上传 | 单页与多页 PDF 可上传，原文件保留，状态从转换中进入成功或失败；其他格式得到明确提示 |
| A03 | 一页一篇新闻 | 上传已知页数的 PDF，成功后生成同等数量新闻，页码与原稿顺序一致 |
| A04 | 转换效果 | 用学院真实稿件对照检查文字、图片、分页和版式；目标屏幕上内容清晰完整，缩略图对应正确 |
| A05 | 失败处理 | 损坏文件、错误格式和转换超时有明确提示，失败任务生成的未完成内容保持隔离 |
| A06 | 上传后预览 | 可查看每页高清图；新生成新闻处于下架状态，管理员加入后出现在播放列表 |
| A07 | 单篇加入与下架 | 新加入新闻位于列表末尾；重复加入保持唯一；下架后移出播放列表，历史记录仍可查询 |
| A08 | 全部加入播放 | 所有转换成功的下架新闻追加至现有列表，原播放项与顺序保持一致，反馈实际新增数量 |
| A09 | 全部下架 | 确认后播放列表为空，大厅自动显示默认宣传画面 |
| A10 | 拖拽排序 | 保存后刷新后台仍保持新顺序，大厅同步后按同一顺序循环播放 |
| A11 | 跨日期播放 | 保留昨日部分新闻、加入今日新闻，确认二者共同播放，日期筛选可查到对应历史记录 |
| A12 | 删除 | 删除单篇清理对应记录与图片；删除来源文件清理其关联内容；播放列表同步更新且其余新闻正常 |
| A13 | 大厅画面 | 1920 × 1080 环境全屏显示，画面仅包含新闻、背景和配置的 Logo，鼠标隐藏；竖版稿件等比例完整显示 |
| A14 | 循环与时间 | 默认每页停留 10 秒；末页后回到首页；单页列表稳定显示；修改时间后按新值播放 |
| A15 | 动画设置 | 淡入淡出、动画关闭及动画时长修改均生效，切换过程正常 |
| A16 | Logo | 上传、更换、开关和四角位置设置生效，图片保持比例 |
| A17 | 默认宣传画面 | 初始空列表可显示内置图；上传替换后自动生效；加入新闻后恢复新闻轮播 |
| A18 | 自动同步 | 后台上下架、排序和配置变化，在下一次约 30 秒轮询获取；资源就绪后的下一个切换点应用，保持自动播放 |
| A19 | 短时网络异常 | 已加载页面遇到短时请求失败时继续播放可用内容；网络恢复后后续轮询正常同步 |
| A20 | 持久化 | 重启应用后，新闻状态、排序、配置、原文件与图片保留并可正常使用 |
| A21 | 开机运行 | 大厅电脑重启后按配置自动打开 Edge 全屏展示页并开始播放 |
| A22 | 连续运行 | 联调覆盖至少一个完整工作日的连续轮播，试运行约一周记录现场结果；无持续白屏、轮播停滞或影响使用的资源增长 |

验收记录包含测试日期、部署环境、稿件名称与页数、预期结果、实际结果、问题及复测结论。

## 15. 交付内容

1. Vue 3 前端及 Spring Boot 后端完整源代码。
2. MySQL 四表初始化脚本和默认配置初始化方式。
3. 可运行的构建产物，以及依赖版本、配置、启动和构建说明。
4. PDF 渲染环境、存储目录与大厅开机全屏操作说明。
5. 默认宣传图片及展示所需基础资源。
6. 联调测试记录、验收结果和试运行问题处理记录。

项目完成标准：管理员能够上传并管理学院新闻，大厅电脑能够按设定顺序和效果自动循环展示，后台变更自动同步，部署及开机运行流程经过现场验证。
